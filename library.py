def calculation_of_the_cube_volume(side_length: float) -> float:
    result = side_length ** 3
    return result


def calculation_of_the_pyramid_volume(area_pyramid: float, height_pyramid: float) -> float:
    result = area_pyramid * height_pyramid
    return result
